package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Calming Premium Dark Palette (Eye-safe for 2 AM study sessions)
val DarkBackground = Color(0xFF0D0F14) // Deep Midnight Obsidian
val DarkSurface = Color(0xFF161A24)    // Soothing Slate Gray
val DarkSurfaceCard = Color(0xFF1E2433) // Lighter Slate Blue for Cards

// Calming Primary Gradients
val PrimaryIndigo = Color(0xFF7F7FD5)  // Healing Indigo
val SecondaryLavender = Color(0xFF86A8E7) // Cloud Lavender
val TertiaryBlue = Color(0xFF91EAE4)      // Soft Turquoise Teal

// Wellness indicators
val ResilientGreen = Color(0xFF42E6A4) // Soft Teal Green for stable states
val CautionYellow = Color(0xFFFFD166)  // Gentle Amber for early caution
val AtRiskOrange = Color(0xFFF28F3B)   // Soft Orange for warning
val CrisisRed = Color(0xFFEF5350)      // Balanced Rose Red for crisis alerts

// Accent Colors
val AccentAmethyst = Color(0xFFB39DDB)
val SoftRose = Color(0xFFFFB7B2)
val TextPrimary = Color(0xFFF3F4F6)
val TextSecondary = Color(0xFF9CA3AF)
val TextMuted = Color(0xFF6B7280)
val GlassBorder = Color(0x1FFFFFFF) // Transparent white for frosting
